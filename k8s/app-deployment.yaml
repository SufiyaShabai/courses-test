apiVersion: apps/v1
kind: Deployment
metadata:
  name: courses-code
spec:
  replicas: 1
  selector:
    matchLabels:
      app: courses-code
  template:
    metadata:
      labels:
        app: courses-code
    spec:
      containers:
        - name: courses-code
          image: rahul9198/courses-code:IMAGE_TAG
          ports:
            - containerPort: 8080
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: mysql-configmap
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: mysql-configmap
                  key: DB_PORT
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: mysql-configmap
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: DB_PASSWORD
